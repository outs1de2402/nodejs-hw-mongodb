tags:
  - Contacts
summary: Отримати контакт по id
operationId: getContactById
parameters:
  - in: path
    name: contactId
    required: true
    schema:
      type: string
responses:
  200:
    $ref: '../../../components/responses/200Contact.yaml'
  400:
    $ref: '../../../components/responses/400.yaml'
security:
  - bearerAuth: []
