get:
  tags:
    - Contacts
  summary: Отримати список контактів
  operationId: getAllContacts
  description: Повертає список усіх контактів користувача
  security:
    - bearerAuth: []
  responses:
    '200':
      description: Успішна відповідь
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../../components/contact.yaml
