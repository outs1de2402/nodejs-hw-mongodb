description: Контакт видалено
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          type: integer
          example: 204
        message:
          type: string
          example: Contact successfully deleted!
